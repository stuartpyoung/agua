{
	name: "plugins.login.Controller",

	version : "0.01",

	url : '',

	description : "Display a login dialogue window and authenticate with the server to retrieve a session ID which is stored as Agua.cookie('sessionId')",

	dependencies :
	[
		{	name: "plugins.core.Controls", version: 0.01	}
	]
}
